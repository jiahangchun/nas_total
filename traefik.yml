## STATIC CONFIGURATION
log:
  level: DEBUG
#  format: json
#  filePath: ./traefik.log


api:
  insecure: true
  dashboard: true

# 开放端口
entryPoints:
  http:
    address: ":9001"
  https:
    address: ":1443"
  mysql:
    address: ":3307"
  ping:
    address: ":8082"


ping:
  entryPoint: "ping"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: true

# volumes 权限问题无法解决
# - $CONFIG/acme:/etc/traefik/acme
#certificatesResolvers:
#  default:
#    acme:
#      email: jiahangchun@163.com
#      storage: /etc/traefik/acme/acme.json
#      certificatesDuration: 72
#      dnsChallenge:
#        provider: tencentcloud
